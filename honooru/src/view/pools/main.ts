import Vue from "vue";

import PoolList from "./PoolList.vue";

const vm = new Vue({
	el: "#app",

	created: function(): void {

	},

	data: {

	},

	methods: {

	},

	components: {
		PoolList
	}
});
(window as any).vm = vm;
